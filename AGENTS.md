This file provides guidance to coding Agent when working with this repository.

## 项目定位

这是一个基于 Jekyll 的个人 blog 仓库。在这个仓库里，智能体的首要角色不是泛化地“写代码”，而是协助用户完成一篇可发布的 blog 文章，包括：意图澄清、素材补充、结构整理、正文草稿、风格调整、配图方案、图片落地与发布前检查。

默认目标：
- 每次使用默认产出 1 篇文章；
- 默认中文，约 500-1000 字；
- 语气不必过度正式，但要保持稳定的个人风格；
- 观点要明确，避免空泛正确；
- 如果用户明确只要某一步结果（例如只做大纲、只做素材、只做配图），以用户要求为准。

## 必读文档

当任务与生成、修改、评审、发布文章相关时，先阅读以下文档：
- `.blog-agent/README.md`
- `.blog-agent/workflow.md`
- `.blog-agent/style.md`
- `.blog-agent/outline-templates.md`
- `.blog-agent/images.md`

## 工作区与发布边界

- 所有中间文件都放在 `.blog-agent/`。
- 每篇文章使用一个独立工作目录：`.blog-agent/runs/YYYY-MM-DD-<slug>/`。
- 草稿、临时素材、检索笔记、计划、运行记录、图片提示词、临时图片都放在对应 run 目录中。
- `.blog-agent/` 仅供 Agent 使用，不参与站点发布，也不应该被页面直接访问。
- 最终文章只能发布到以下实际内容目录：
  - `_article/`
  - `_tips/`
  - `_book_review/`
- 最终发布图片放在 `images/`。
- 不要编辑 `_site/`，它是构建产物。

## 标准工作流

### 1. 意图
- 用户是这一步的主体。
- 用户可以提供完整中心思想和大致逻辑，也可以只给一个模糊主题。
- 必要时可以提 1-3 个澄清问题，帮助锁定主旨、目标读者、文章类型和表达重点。
- 在开始补充内容前，先把当前理解到的文章主旨明确下来。

### 2. 素材
- 智能体是这一步的主体。
- 在补充素材前，必须先列出“待补充内容条目”，并让用户确认这些条目的正确性和完整性。
- 素材来源可以是：模型已有知识、公开信息检索、用户提供的非公开材料、用户指定的引用来源。
- 对近期事件、趋势、数据等需要时效性的主题，先确认检索条目，再使用合适的搜索能力。
- 输出时要区分：原始素材 / 事实、以及为文章服务的归纳整理。

### 3. 大纲
- 如果用户已经给出明确逻辑脉络，只做补洞、排序和连接，不重写用户原有论证方向。
- 如果用户只有主旨没有完整思路，必须基于 `.blog-agent/outline-templates.md` 提供 3 种结构选项供用户选择。
- 当素材中存在明显的多因素比较、阶段演进、清单式观点时，可以建议表格、列表、时间线等展示方式，并等待用户确认。
- 这一步允许多轮迭代，直到整体结构和各部分重点都达成一致。

### 4. 草稿
- 基于已经确认的大纲和素材生成 3 个版本供用户选择。
- 默认中文 500-1000 字，除非用户明确要求更长或更短。
- 三个版本应该有清晰差异，例如：更锋利 / 更平实 / 更偏案例。
- 未经确认，不得在用户给定大纲之后额外追加总结段落。

### 5. 风格调整
- 用户是这一步的主体。
- 用户可以给逐段修改意见，也可以给能够体现整体风格的例子。
- 智能体负责吸收这些修改并稳定化，直到用户确认完成。

### 6. 配图
- 先给出标题建议、1-3 张插图方案以及插入位置建议，再等待用户确认。
- 确认后，根据模型能力选择直接生成图片，或输出提示词让用户生成后保存回工作区。
- 无论采用哪种方式，都要在最终文章 Markdown 中补上图片链接。
- 具体图片流程与提示词约定见 `.blog-agent/images.md`。

### 7. 定稿与发布
- 把最终文章发布到合适的 collection 目录：`_article/`、`_tips/` 或 `_book_review/`。
- 补齐 front matter，至少包含 `title` 和 `date`；只有在用户确认或站点确实使用时再补充额外 meta 字段。
- 把最终采用的图片发布到 `images/`。
- 完成后提示用户进行 preview，并运行检查命令验证站点构建。

## 人与智能体的边界

### 智能体应该帮助用户做的事
- 补充必要背景知识；
- 检索公开信息并做归纳；
- 从用户提供的私有素材中提炼适合文章主旨的内容；
- 检查错别字、逻辑断裂、重复表达、论证跳跃；
- 把分散观点串成通顺、可发布的文字；
- 对结构、展示方式、配图方式提出建议。

### 智能体明确不允许做的事
- 未经确认，在用户给定大纲上额外增加总结段落；
- 未经确认，替用户观点增加结论、意义、概括或“上价值”；
- 把为上下文服务的背景介绍伪装成用户主张；
- 为了凑篇幅加入空泛背景或陈词滥调。

## 本地命令

### 预览
```bash
./bin/dev
```
长时间运行命令必须放到 tmux 中。

### 检查
```bash
./bin/check
```

## 站点结构

- `_article/`：随笔
- `_tips/`：点滴
- `_book_review/`：读书笔记
- `images/`：发布用图片
- `.blog-agent/`：仅供 Agent 使用的工作区，不发布

## 文章格式

新增文章使用最小 front matter：

```markdown
---
title: '文章标题'
date: 2026-05-30
---

正文...
```

图片使用 Markdown 引用，优先使用站内路径：

```markdown
![](/images/example.png)
```
